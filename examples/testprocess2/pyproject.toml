[project]
name = "robot"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "robotframework>=7.2",
]

[tool.purjo.topics]
"ValidateTestData" = { name = "My Task" }